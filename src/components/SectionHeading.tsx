export function SectionHeading({eyebrow,title,copy}:{eyebrow:string;title:string;copy?:string}){return <div className="mb-9 max-w-3xl"><p className="mb-2 text-sm font-bold uppercase tracking-[.22em] text-teal-600 dark:text-teal-400">{eyebrow}</p><h2 className="text-3xl font-black tracking-tight md:text-4xl">{title}</h2>{copy&&<p className="mt-4 leading-7 text-slate-600 dark:text-slate-300">{copy}</p>}</div>}
